#include "Assembler.hpp"
using namespace shasta;

void Assembler::exploreDirectedReadGraph(
    const vector<string>& request,
    ostream& html)
{
    html << "Display of the directed read graph not implemented.";
}
