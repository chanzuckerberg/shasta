#ifndef SHASTA_ALGORITHM_HPP
#define SHASTA_ALGORITHM_HPP

#include <algorithm>

namespace shasta {
    using std::copy;
    using std::fill;
    using std::find;
    using std::max;
    using std::min;
    using std::sort;
    using std::swap;
    using std::unique;
}

#endif

