#ifndef CZI_SHASTA_IOSTREAM_HPP
#define CZI_SHASTA_IOSTREAM_HPP

#include <iostream>

namespace ChanZuckerberg {
    namespace shasta {
        using std::cin;
        using std::cerr;
        using std::cout;
        using std::dec;
        using std::endl;
        using std::flush;
        using std::hex;
        using std::istream;
        using std::ostream;
    }
}

#endif
