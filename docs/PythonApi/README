To create html documentation for the Python API:

- Make sure sphinx is installed.
- Set PYTHONPATH to point to a directory containing shasta.so.
  Sphinx uses the documentation strings stored there.
- In this directory, do "make cclean; make html".
- The output goes to _build/html
